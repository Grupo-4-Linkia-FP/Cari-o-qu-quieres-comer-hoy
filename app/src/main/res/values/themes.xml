<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.CQQCH" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Personaliza aquí el tema claro -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary_variant</item>
        <item name="colorSecondary">@color/my_light_secondary</item>
        <item name="colorAccent">@color/my_light_accent</item>
    </style>

    <!-- Tema principal de la aplicación -->
    <style name="Theme.CQQCH" parent="Base.Theme.CQQCH">
        <!-- Barras del sistema transparentes para el diseño de borde a borde -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Ajustar el color de los iconos en la barra de estado dependiendo del tema -->
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>
</resources>
